import React, {Component} from 'react';
import { Connect } from 'react-redux';


class PlayerDetail extends Component{
    state={
        // players:[],
        // nPCs:[],
    }
    render(){
        return(
            <h2>this is the PlayerDetail component</h2>
        )
    }
}

export default PlayerDetail