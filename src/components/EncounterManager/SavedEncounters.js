import React, {Component} from 'react';
import { Connect } from 'react-redux';


class SavedEncounters extends Component{
    state={
        // players:[],
        // nPCs:[],
    }
    render(){
        return(
            <h2>this is the saved encounter component</h2>
        )
    }
}

export default SavedEncounters